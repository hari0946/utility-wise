databases:
  - name: utility-wise-db
    databaseName: utility_wise
    user: utility_wise
    plan: free

services:
  - type: web
    name: utility-wise-api
    env: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.10"
      - key: DATABASE_URL
        fromDatabase:
          name: utility-wise-db
          property: connectionString
      - key: ADMIN_API_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: RATE_LIMIT_SECONDS
        value: "30"

  - type: web
    name: utility-wise-frontend
    env: static
    rootDir: frontend
    buildCommand: ""
    staticPublishPath: .
